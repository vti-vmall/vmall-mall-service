spring:
  application:
    name: vmall-mall-service
  datasource:
    url: jdbc:mysql://localhost:3306/vmall_mall
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8081

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

